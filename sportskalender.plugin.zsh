# myplugin.plugin.zsh

function sport() {
  # Change to the script's directory
  cd "$(dirname "$0")"

  node hoydepunkter.js
}

function fotball() {
  # Change to the script's directory
  cd "$(dirname "$0")"

  node fotball.js
}